#pragma once

enum costumeType
{
	HAT,
	CLOTH,
	TROUSER,
	SHOES,
	TEST,

	//////////////////////////////
	//为了赶比赛添加的不友好的定义
	//////////////////////////////

	SCREENSHOT_END,
	DEFAULT
};

enum QT_CONTROL
{
	ON_OFF, //打开或者关闭界面
	LEFT, //左
	RIGHT, //右
	UP, //上
	DOWN, //下
	SCREENSHOT, //拍照
	EXIT  //退出
};